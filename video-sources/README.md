# Local video sources

This directory keeps the original project recordings organized outside the
production asset bundle. MP4 recordings below this folder are ignored by Git;
only optimized films intended for the website belong in `assets/videos/`.
