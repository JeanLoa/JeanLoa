{
  "name": "jean-loa-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "generate:data": "node scripts/generate-project-data.mjs",
    "refresh:data": "npm run generate:data",
    "build": "node scripts/build.mjs"
  }
}
